global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: f1-metrics-exporter
    static_configs:
      - targets:
          - metrics-exporter:8000
    metrics_path: /metrics
